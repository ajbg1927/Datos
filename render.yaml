services:
  - type: web
    name: backend-flask
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 4 -b 0.0.0.0:$PORT backend.app:app"
    envVars:
      - key: DATABASE_URL 
        sync: false